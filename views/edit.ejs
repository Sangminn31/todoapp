<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <link rel="stylesheet" href="/main.css">
</head>
<body>
    <%- include('nav.ejs') %>
    <div class="container">
        <div class="form-box">
            <h1>Edit Post</h1>
            <form action="/edit?_method=PUT" method="POST">
                <input type="hidden" name="id" value="<%= result._id %>">
                <input type="text" name="title" placeholder="Title" value="<%= result.title %>" required>
                <textarea name="content" placeholder="Content" required><%= result.content %></textarea>
                <br>
                <button type="submit">Update Post</button>
            </form>
        </div>
    </div>
</body>
</html>